enclave {
    from "sgx_tstdc.edl" import *;

    trusted {
        /* define ECALLs here. */
        public void ecall_printHello(void);
    };

    untrusted {
        /* define OCALLs here. */
        void ocall_printString([in, string] const char *str);
    };
};
